services:
  - type: web
    name: mammogram-classifier
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn mammogram_project.wsgi"
    envVars:
      - key: DEBUG
        value: False
      - key: RENDER
        value: True
